{
  "name": "oneindex-docker-heroku部署",
  "description": "Deploy oneindex to Heroku.",
  "keywords": ["oneindex"],
  "env": {
    "REFRESH_TOKEN": {
      "description": "使用crontab进行token更新，默认`0 * * * *`，即每小时更新一次",
      "value": "0 * * * *"
    },
    "REFRESH_CACHE": {
      "description": "使用crontab进行缓存更新，默认`*/10 * * * *`，即每10分钟更新一次",
      "value": "*/10 * * * *"
    }
  },
  "website": "",
  "repository": "",
  "stack": "container"
}